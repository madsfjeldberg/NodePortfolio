<header class="bg-dark-surface shadow-md border-b border-dark-border mb-8">
  <div class="container mx-auto px-4 py-4">
    <div class="flex flex-col md:flex-row justify-between items-center">
      <div class="flex items-center mb-4 md:mb-0">
        <h1 class="text-2xl font-bold text-dark-accent"><a
          href="/"
          class="text-dark-accent hover:text-white font-bold transition-colors"
        >
          NodePortfolio
        </a></h1>
      </div>
      <nav>
        <ul class="flex flex-wrap gap-2 md:gap-6">
          <li>
            <a
              href="/"
              class="<%= currentPath === '/' ? 'text-white after:scale-x-100' : 'text-dark-accent' %> hover:text-white font-medium transition-colors relative after:absolute after:bottom-[-2px] after:left-0 after:w-full after:h-0.5 after:bg-white after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300"
            >
              Frontpage
            </a>
          </li>
          <li>
            <a
              href="/restapi"
              class="<%= currentPath === '/restapi' ? 'text-white after:scale-x-100' : 'text-dark-accent' %> hover:text-white font-medium transition-colors relative after:absolute after:bottom-[-2px] after:left-0 after:w-full after:h-0.5 after:bg-white after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300"
            >
              REST API
            </a>
          </li>
          <li>
            <a
              href="/javascript"
              class="<%= currentPath === '/javascript' ? 'text-white after:scale-x-100' : 'text-dark-accent' %> hover:text-white font-medium transition-colors relative after:absolute after:bottom-[-2px] after:left-0 after:w-full after:h-0.5 after:bg-white after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300"
            >
              JavaScript
            </a>
          </li>
          <li>
            <a
              href="/nodejs"
              class="<%= currentPath === '/nodejs' ? 'text-white after:scale-x-100' : 'text-dark-accent' %> hover:text-white font-medium transition-colors relative after:absolute after:bottom-[-2px] after:left-0 after:w-full after:h-0.5 after:bg-white after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300"
            >
              Node.js
            </a>
          </li>
          <li>
            <a
              href="/express"
              class="<%= currentPath === '/express' ? 'text-white after:scale-x-100' : 'text-dark-accent' %> hover:text-white font-medium transition-colors relative after:absolute after:bottom-[-2px] after:left-0 after:w-full after:h-0.5 after:bg-white after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300"
            >
              Express
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </div>
</header>

<style>
  /* Global styles for inline code */
  .shiki-inline {
    font-family: "Consolas", "Monaco", "Andale Mono", "Ubuntu Mono", monospace;
    font-size: 0.9em;
    vertical-align: middle;
    border-radius: 0.25rem;
  }

  /* Ensure code blocks have proper spacing */
  pre.shiki {
    margin: 1rem 0;
    padding: 1rem;
    overflow-x: auto;
  }

  /* Ensure proper spacing in lists with inline code */
  li code {
    margin: 0 0.2em;
  }
</style>